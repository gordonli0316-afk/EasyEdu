# ============================================
# EasyEdu — web application runtime
# Only what the FastAPI app needs to boot and serve
# the tutoring workflow. Install with:
#     pip install -r requirements.txt
# Optional features (OCR, PDF/Word/Excel export, model
# training) live in requirements-optional.txt.
# ============================================

# Web framework
fastapi>=0.110,<0.120
uvicorn[standard]>=0.27
jinja2==3.1.6
python-multipart>=0.0.9
pydantic>=2.6

# Agent workflow (LangGraph orchestration + message types)
langchain-core>=0.3.49
langgraph>=0.3.30
typing_extensions>=4.9

# LLM access: our own httpx client speaking the OpenAI-compatible protocol
httpx>=0.27
python-dotenv>=1.0
