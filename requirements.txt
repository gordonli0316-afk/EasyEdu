# ============================================
# EasyEdu - 依赖包列表
# ============================================

# 核心依赖（必需）
uvicorn>=0.15.0
fastapi>=0.68.0
python-multipart>=0.0.5  # 文件上传必需
pydantic>=1.8.0
jinja2==3.1.6

# LLM和AI相关
langchain==0.3.23
langchain-openai==0.3.12
langgraph==0.3.30
transformers>=4.50.0
tiktoken>=0.3.3

# 数据处理
numpy>=1.20.0
pdfplumber>=0.7.0
PyMuPDF>=1.18.0

# 文件处理和OCR（用于文件上传功能）
Pillow>=9.0.0  # 图片处理（必需）
pdf2image>=1.16.0  # PDF转图片（必需，需要系统级poppler-utils）
paddleocr>=2.6.0  # OCR识别（可选，如果使用图片上传功能）

# 文件导出（用于题目导出功能，可选）
openpyxl>=3.0.0  # Excel导出（可选）
python-docx>=0.8.11  # Word导出（可选）
reportlab>=3.6.0  # PDF生成（可选）

# 工具依赖
python-dotenv>=0.19.0
tqdm>=4.62.0
requests>=2.25.0
httpx>=0.27.0
python-dateutil>=2.8.0  # 日期处理（用于抽认卡、番茄钟）

# ============================================
# 系统级依赖（需要单独安装）
# ============================================
# poppler-utils  # PDF转图片需要
#   Ubuntu/Debian: sudo apt-get install poppler-utils
#   macOS: brew install poppler
#   Windows: 下载安装包或使用conda: conda install -c conda-forge poppler

# ============================================
# 可选依赖 - 根据需要取消注释
# ============================================
# 自有模型（训练 / 推理，GPU 机器安装）
# torch>=2.0.0
# transformers>=4.50.0
# peft>=0.10.0
# bitsandbytes>=0.43.0
# accelerate>=0.30.0
# vllm>=0.8.0